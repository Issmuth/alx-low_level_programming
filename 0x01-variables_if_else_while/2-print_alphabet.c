#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always success (0)
 */
int main()
{
	int a = 65;

	while (a < 92)
	{
		putchar('%d + '0'', a);
		a++;
	}
	return (0);
}
