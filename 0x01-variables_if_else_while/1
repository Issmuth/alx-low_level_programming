#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * except the letters e and q
 *
 * Return: o if successful 1 otherwise
 */
int main(void)
{
	char a = 'a';

	while(a <= 'z')
	{
		if(a != 'e' || a != 'q')
		{
			putchar(a);
		}
	}
	putchar('\n');
	return (0);
}	
