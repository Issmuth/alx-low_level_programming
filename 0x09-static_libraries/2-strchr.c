#include "main.h"

/**
 * main - search
 * @s: string to search
 * @c: character to be found
 *
 * Return: pointer to the character's first occurence
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
