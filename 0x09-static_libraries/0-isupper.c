#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * main - isupper function
 * @c: character to be checked
 *
 * Return: 0 if not uppercase 1 otherwise
 */

int main(char c)
{
	int x;

	x = isupper(c);
	return (x);
}
